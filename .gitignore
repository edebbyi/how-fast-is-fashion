# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/

# Environment
.env
.venv/
venv/
env/

# Data — images are too large for git (hosted separately)
data/01_data_audit/raw_images/
data/02_reference_corpus/raw/
data/02_reference_corpus/boards/
data/04_training/*.jsonl
*.parquet

# Scraped-and-approved reference images: copyrighted editorial photography,
# not ours to redistribute like the hand-picked references. Kept local-only;
# candidates.jsonl (source URL/hash provenance) is still committed.
data/02_reference_corpus/labeled/*/scraped_*
data/02_reference_corpus/phashes.json
data/02_reference_corpus/.last_sync

# Vector DB binary files — rebuildable from scripts/embed_reference_corpus.py
data/02_reference_corpus/qdrant/

# Data — these ARE committed for reproducibility:
# data/01_data_audit/records/*.jsonl       (raw metadata audit trail)
# data/01_data_audit/cleaned/*.jsonl       (filtered dataset)
# data/01_data_audit/state/*.json          (mining state)
# data/01_data_audit/normalization/        (LLM outputs + prompts)
# data/01_data_audit/evaluation/           (gold set + run results)
# data/03_shared/taxonomy/                 (trend definitions)

# Notebooks
.ipynb_checkpoints/
*.ipynb_metadata/

# MLflow
mlruns/
mlartifacts/
mlflow.db

# IDE
.vscode/
.idea/
.qodo/
.zencoder/
.zenflow/

# OS
.DS_Store
Thumbs.db

# Docker
.dockerignore

# Misc
*.log
*.tmp

# Local-only working notes, not for the repo
RETROSPECTIVE.md
